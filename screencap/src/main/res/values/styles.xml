<resources>

    <style name="ScreencapApiTransparent"  parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/full_transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/screencap_api_animation_translucent_translate</item>
    </style>

    <style name="screencap_api_animation_translucent_translate" parent="@android:style/Animation.Translucent">
        <!-- B 进入 -->
        <item name="android:activityOpenEnterAnimation">@anim/fade_id</item>
        <!-- A 退出 -->
        <item name="android:activityOpenExitAnimation">@anim/anim_hold</item>
        <!-- B 退出 -->
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
        <!-- A 进入 -->
        <item name="android:activityCloseEnterAnimation">@anim/anim_hold</item>
    </style>
</resources>
